import React from 'react';

// Bootstrap component
import { Alert } from 'reactstrap';

function Title() {
    return (
        <Alert color="info">
            <h2>ReactJS Hangman Game</h2>
        </Alert>
    )
}


export default Title;